#ifndef __STRINGUTILS_H__
#define __STRINGUTILS_H__

#include <stdlib.h>

size_t StringLength(char* string, size_t maxlen);
void StringLimit(char* string, size_t maxlen);

#endif /* __STRINGUTILS_H__ */
